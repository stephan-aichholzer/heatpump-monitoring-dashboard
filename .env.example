# ==============================================================================
# Heat Pump Dashboard - Environment Variables
# ==============================================================================
# Copy this file to .env and adjust values for your setup
# Usage: cp .env.example .env
# ==============================================================================

# Currently this stack uses Docker service names for all connectivity
# No external IP addresses are required - all communication happens via Docker networks

# Port Configuration (if you need to change default ports)
# PROMETHEUS_PORT=9090
# GRAFANA_PORT=3000
# MODBUS_EXPORTER_PORT=9100

# ==============================================================================
# Network Topology
# ==============================================================================
# Data Flow:
#
# External Sources → Prometheus (metrics collection):
#   ├─ modbus_exporter:9100 (heatpump_dashboard_default network)
#   │  └─ WAGO/Modbus device metrics
#   │
#   ├─ iot-api:8000/metrics (shelly_bt_temp_default network)
#   │  └─ Shelly BT temperature sensor metrics
#   │
#   └─ lg_r290_service:8000/metrics (heatpump_dashboard_default network)
#      └─ LG R290 heat pump metrics
#
# Prometheus → Grafana (visualization)
#   └─ Dashboards, alerts, and data exploration
#
# ==============================================================================
# Prometheus Scrape Targets (configured in prometheus/prometheus.yml)
# ==============================================================================
# - job 'wago': modbus_exporter:9100
# - job 'temperature-sensors': iot-api:8000/metrics
# - job 'heatpump': lg_r290_service:8000/metrics
# ==============================================================================

# Data Retention
# Prometheus is configured for 1 year retention (--storage.tsdb.retention.time=1y)
# Modify in docker-compose.yml if different retention is needed

# External Access
# Prometheus UI: http://<raspberry-pi-ip>:9090
# Grafana UI: http://<raspberry-pi-ip>:3000
